export interface InterfaceProductCard {
  title: string; 
  description: string;
  price: number;
  image: string; // Nome do arquivo da imagem (ex: 'bolo-brigadei // Quantidade de pessoas que serve
} 